<%= form_for ([@task, @task.comments.build]) do |f| %>

<p>
  <%= f.label :commenter %>
  <%= f.text_field :commenter %>
</p>

<p>
  <%= f.label :Comment %>
  <%= f.text_field :body %>
</p>

<%= f.submit %>

<% end %>