<%= form_for @task do |f| %>
<%# form_for knows whether to 'update' or 'create' %>

<% if @task.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@task.errors.count, "error") %> prohibited
      this task from being saved:</h2>
    <ul>
    <% @task.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

<p>
<%= f.label :title %><br>
<%= f.text_field :title %>
</p>

<p>
<%= f.label :description %><br>
<%= f.text_field :description %>
</p>

<p>
<%= f.label :owner %><br>
<%= f.text_field :owner %>
</p>

<%= f.submit %>

<% end %>